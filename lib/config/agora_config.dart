// Archivo de configuración Agora - funcionalidad de llamadas deshabilitada
// Este archivo puede ser eliminado si no se usa funcionalidad de llamadas

class AgoraConfig {
  // Configuración deshabilitada
  static const String appId = '';
  static const bool isEnabled = false;
}